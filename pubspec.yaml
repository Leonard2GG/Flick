name: flick
description: "A modern movie discovery app."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5       # Para manejar la lista de "Ver más tarde"
  flutter_card_swiper: ^2.0.3 # Para el efecto tipo Tinder

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
